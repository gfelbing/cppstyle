/**
 * @brief This is a brief description of Foo. It is good that is
 * has a brief comment, but it is way to long. In our test it should be 80 chars at max.
 *
 * It has no more use than to be an example for
 * commented code.
 *
 * It has a very huge comment.
 */
class Foo {
public:
    /**
     * @param foo is a parameter with a too long description.
     * @param baz description.
     */
    int method(int foo, int fooBar, int baz);
}

/**
 * @brief wonderful short brief comment.
 */
class Bar {
}
